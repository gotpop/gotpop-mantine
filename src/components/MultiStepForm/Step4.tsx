import { Container, Radio, TextInput } from "@mantine/core"
import { useStyles } from "./useStyles"

type Props = {
  form: any
}

export default function Step4({ form }: Props) {
  const { classes } = useStyles()

  return (
    <>
      <h1>test</h1>
    </>
  )
}
